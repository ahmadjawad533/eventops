import { prisma } from "../../core/prisma";

export class CollaborationRepository {
  // Data access methods for collaboration
}

export const collaborationRepository = new CollaborationRepository();
